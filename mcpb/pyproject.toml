[project]
name = "tappi-mcpb"
version = "0.5.1"
description = "tappi MCP server for Claude Desktop — browser control via CDP"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
    "websockets>=12.0",
]
